{
  "name": "monorepo-template",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "backend",
    "common",
    "frontend"
  ],
  "scripts": {
    "database:up": "docker-compose -f docker-compose.dev.yml up -d",
    "database:down": "docker-compose -f docker-compose.dev.yml down"
  }
}
